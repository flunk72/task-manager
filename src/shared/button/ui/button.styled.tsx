import styled from 'styled-components';
import * as antd from 'antd';

export const ButtonStyled = styled(antd.Button)`
  font-family: var(--font-family);
  color: var(--white);
  background: var(--background-button);
  border-color: var(--border-color);
  margin-left: 15px;
`;
